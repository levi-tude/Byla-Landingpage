# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
linter.log

# Dependencies
node_modules
dist
dist-ssr
*.local

# Cursor — nunca versionar MCP com chaves (se existir na pasta do projeto)
.cursor/mcp.json

# Clone aninhado (tem próprio .git) — não versionar como pasta vazia
n8n-mcp-server/

# Metabase local (JARs > 100MB — GitHub bloqueia)
metabase/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env e credenciais (nunca versionar)
.env
.env.*
!.env.example
**/service-account*.json
**/*.credentials.json
backend/.env
backend/n8n-byla-14b9ea6e929d.json
